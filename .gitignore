# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/
build/
dist/

# env files (never commit secrets)
.env
.env*.local

# db
*.db
*.db-journal
prisma/dev.db

# misc
.DS_Store
*.log

# git worktrees (checked out working copies, not repo content)
.claude/worktrees/

# user-uploaded content
public/uploads/

# typescript build cache
*.tsbuildinfo

# raw AI-generated marketing image source material (126MB of full-res PNGs
# and duplicates) — optimized copies actually used by the site are checked
# in under public/images/; this is just the source package, not app content
/interior_design_website_assets/
/*.png
/Interior Design Website Image Set.md
/asset-manifest.csv
